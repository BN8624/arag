# 루트 pytest 설정 — bare `pytest`가 runs/의 생성물(test_acceptance.py 동명 161개 충돌)을 긁지 않게
# 수집 범위를 진짜 스위트 tests/ 로 한정한다. game/ 레퍼런스 테스트는 `cd game && pytest`로 따로 실행.
[pytest]
testpaths = tests
norecursedirs = runs .git game
