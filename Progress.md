# Progress: 자가개선 인프라(0~3층) + 부분합격·중재·자동개선 루프 완료

## 현황 요약 (2026-06-12 갱신, 7차 세션)
- **상태**: 테스트 189개 통과. 밤샘 배치 18회차 완주(13 OK) 분석 완료
  → 배치에서 나온 실패 4종에 대한 수정 반영 (아래 "배치 분석 결과와 수정")
- **대시보드 가동 중**: 모드 버튼 단일/개선/자동(회차 1~20)/종료예약 전부 작동
- 두 모델 콜 테스트 정상 (gemma-4-26b-a4b-it / gemma-4-31b-it 둘 다 PONG + JSON OK)

## 7차 세션 마무리: 하네스 글 반영 — 평가자 노트 + improve 체인 (2026-06-12)

Anthropic "장시간 에이전트 하네스 설계" 글 검토 → 코덱스와 3자 합의.
핵심 채택: **평가자도 튜닝 대상이다** + **각 하네스 부품이 진짜 필요한지 계속 검증한다**.

### 평가자 노트 (`evaluator_notes.py` → evaluator_mistakes.json) — 세 번째 노트
- 분담: lessons=26B 실패 예방(설계 주입) / critique_notes=구현 개선(26B 주입) /
  **evaluator_mistakes=31B 평가자 교정 자료 (수집·표시만, 주입 안 함 — 합의)**
- 수집 4종(콜 0, 기존 기록에서 추출): false-lgtm(LGTM 후 총평 지적) /
  perfect-but-gap(만점인데 총평 지적 = 시험지 빈틈) / partial-lgtm / repeat-review
- 같은 유형 3회 이상 반복 → 비평/출제 프롬프트 승격 후보로 표시
- analyze_batch에 섹션 추가 + `python evaluator_notes.py` 단독 실행 가능
- **첫 수확: perfect-but-gap 8회** — 만점 런 전원이 총평에서 지적받음.
  지적 8건을 읽어보니 트집이 아니라 실질(약속한 hours 대신 분 출력, 파일명
  하드코딩, 약속한 리포트 파일 미저장 등) = **시험지가 약속을 못 덮는 패턴**
- **즉시 승격 실행**: design_prompt에 약속-커버리지 규칙 추가 — 수용기준은
  "커맨드가 도는가"가 아니라 아이디어의 약속(출력 단위·형식, 범용성, 지원
  문법 범위)을 검증해야 함. 기준 수를 늘리지 말고 덮는 범위를 정렬하라고 명시
  (기준 수 증가 = 게이트 비용 증가 방지)
- 리뷰어 후함 여부는 다음 배치가 공짜 검증: improve 체인 → 재총평 사이클에서
  NOCHANGE가 한 번도 안 나오고 repeat-review만 쌓이면 리뷰어 프롬프트를 조일 것

### improve 체인 (batch) — "개선의 개선 금지" 완화
- 깊이 2까지 허용하되 3중 가드: 직전 판정 IMPROVED(회귀·무이득 체인 중단) +
  같은 failed_criteria 반복이면 중단 + 깊이 한도
- 근거: improve 8건 중 6건 성공·건당 $0.01 미만 (밤 배치 실측) — 1회 제한이 과보수
- 총평 대상도 같은 가드로 확장 (IMPROVED 만점 improve 런은 총평 1회 받을 수 있음)

### 하네스 재검토 원칙 (문서화)
> 모델을 교체하거나 큰 성능 변화가 생기면, 각 가드레일이 여전히 load-bearing인지
> A/B로 재검증한다. 제거 실험은 한 번에 하나씩만 수행한다.
- 현 가드레일 전부가 gemma 4의 실관측 실패에서 나옴 — 모델이 바뀌면 과잉 장치일 수 있음

### 계약 협상 (2차 후보로만 기록)
- 26B 자유 질의가 아니라 "정적 모호성 감지(콜 0) → 걸리면 31B에게 1회 재설계"
  형태가 안전 — covered_by 커버리지 정적 검증과 함께 2차에서 검토

## 7차 세션 후반: 밤샘 배치 18회차 분석 결과와 수정 (2026-06-12)

### 분석 결과 (판정 포인트 5개 — analyze_batch.py 기준)
- **improve 자동 루프 = 최강 효율 확정**: 8건 중 6건 IMPROVED (전부 새 기준까지 통과,
  건당 콜 2~8 / $0.004~0.012). 실패 2건은 동일 원인 (아래 수정 1)
- **신규 성공률 대폭 상승**: 기준선 0/4 전패 → 효율반영 4(+부분합격1)/8
- **부분합격 출하 2건 작동** (신규 1 + 재도전 1) / **중재 2회 발동** (기준선 0회)
- **단언분쟁 런당 절반으로**: 기준선 9건/9런 → 효율반영 8건/18런
- **재발률 첫 집계**: 주입 13런 중 재발 4건 (31%)
- 적응형 난이도가 배치 중 1→4까지 상승 (14회차 level 4) — 레벨 기록이 다음 배치부터
  교란 보정에 쓰임
- 정정: improve 비평 프롬프트 ~30K자는 잘림이 아니라 자연 크기 (설계+코드 전체 포함)

### 분석에서 나온 수정 4종 (테스트 189개 통과)
1. **improve 불량 신규 기준 구제** (orchestrator `_salvage_new_checks` + prompts 규칙)
   - 실관측: 31B가 echo 리다이렉션 채점 커맨드를 재시도에서도 반복 → improve 통째 ABORT 2건
   - 수정: 검증 에러가 "신규 채점 커맨드"에만 있으면 그 기준만 떨구고 진행
     (원본 그대로인 기존 기준이 걸리면 구제 거부 — 회귀 방지선 보호)
   - improve_prompt에 "체크 커맨드는 python 스텝만, 픽스처도 python -c로" 규칙 명시
2. **네트워크 에러 분류 보강** (llm `_is_transient`)
   - 실관측: WinError 10054(연결 절단)가 SDK 예외에 감싸여 재시도 없이 즉사 1건
   - 수정: 메시지 패턴(winerror 10054/connection reset/timed out 등)도 백오프 대상
3. **배치 인프라 브레이커** (batch)
   - 실관측: 새벽 07시 API 500 연쇄에 3회차(improve+출제+신규)를 연달아 태우고 정지
   - 수정: 인프라 장애로 분류된 실패(index status·예외 메시지)는 연속실패와 분리 —
     첫 번은 15분 대기 후 재개, 연속 2회면 "infra-outage"로 정지
4. **improve 프롬프트 다이어트**: 비평 입력에서 시험지 제외 (~5K자 절약 —
   기준·체크는 설계 JSON에 이미 있음)

## 7차 세션: 배치 분석 도구 + 레벨 기록 (2026-06-12)

### 분석 스크립트 `analyze_batch.py` (콜 0, 읽기 전용 — 배치 중에도 안전)
- 단계 자동 구분(커밋 시각 기준): 기준선(<02:11) / 출구반영(02:11~02:39, 부분합격·중재)
  / 효율반영(≥02:39, 다이어트·resume·녹음)
- 런유형 분리 집계: 신규/재도전/improve — improve(콜 3~4)와 재도전(resume 절약)이
  신규 런 평균을 오염시키지 않게
- 판정 포인트 5종 전부 표로: 단언분쟁·시험수리·중재·부분합격·resume(이벤트),
  improve 성과(IMPROVED/NO-GAIN), 다이어트(녹음 prompt_chars + input tok/콜), 재발률
- 첫 수치(표본 12런): 기준선 신규 0/4 전패·재도전이 절반 구제, improve 2/3 성공,
  재발률 첫 집계 주입 5런 중 재발 2건(40%)

### 출제 레벨을 index에 기록 (`--level`)
- batch → orchestrator `--level` 전달 → index 엔트리에 `level` 기록 (재도전에도 따라감)
- 이유: 적응형 난이도가 움직이면 "성공률이 오른 게 개선 덕인지 난이도 하향 덕인지"
  구분 불가 — 전후 비교의 교란 변수를 데이터로 남김
- 가동 중인 배치는 수정 전 프로세스라 **다음 배치 시작분부터 기록됨** (기존 런은 표에 `-`)

### 합의: 산출물 형태 확장 방향 (아직 미착수 — 스코프 보호 유지)
- 현재는 Python CLI만 생산 (설계 프롬프트·성공신호·게이트 전부 CLI 전제)
- 확장 순서 합의: ① 라이브러리(성공신호를 "import+함수 호출 스크립트"로 — 게이트 변경 최소)
  → ② 웹앱(서버 기동+HTTP 검증 게이트 신설 필요) / GUI는 자동 검증 부적합으로 제외
- 단, 착수 시점은 **CLI 신규 생성 성공률을 끌어올린 뒤** — 지금 병목은 형태가 아니라 성공률

## 6차 세션 후반: 효율 개선 7종 (코덱스 리뷰 + 자체 분석 합본)
1. **컨텍스트 다이어트**: 구현=직접 의존 파일만 / 수리·수정=표적+직접 의존+역의존만
   (26B thinking이 비용의 83% — 입력 크기가 주범이라 구조적으로 절감)
2. **비평 기본 1바퀴** (부분 합격이면 2바퀴) — 반복 다듬기는 1/10 비용인 improve 담당
3. **resume 재도전**: 실패 런에 design.json 있으면 설계·시험지 재사용 (5~7콜 절약)
4. **인프라 브레이커**: API 5xx 연속으로 죽은 런은 자동 재도전 생략 (같은 장애에 콜 안 태움)
5. **improve 계획 폴백**: 31B 설계 재출력 실패해도 changes만 멀쩡하면 기존 설계로 진행
   + 실패 원인(no-json/bad-json 등)과 응답 꼬리 로깅 (새벽 ABORT 재발 시 진단 가능)
6. **pytest/채점표 역할 분리 문구**: 채점표=CLI 레벨, pytest=함수·엣지 — 중복 검사 차단
7. **배치 종료 요약**: 회차별 성패·점수·비용·improve 여부 표 출력 (콜 0, 아침 리뷰용)
8. **녹음·재생 (replay)**: 콜마다 응답 전문을 `runs/<run>/llm_calls.jsonl`에 녹음
   (prompt는 크기+머리 300자만 — 컨텍스트 다이어트 효과 측정 겸용).
   `--replay <run_dir>`로 같은 응답을 재생해 콜 0으로 운영 회귀 재현
   (코덱스 반론 수용 — MockLLM은 합성 픽스처, replay는 실제 런 재현이라 역할이 다름.
   녹음 없이는 재현할 과거가 없으므로 녹음기를 먼저 가동)
- 보류: critical criteria(스키마 파급 커서 다음에), 총평 샘플링(만점 런 희귀해 아직 불필요),
  docs 생략(실패 런은 이미 생략 + README는 총평 리뷰어 입력)

## 바로 다음 작업 (우선순위 순)

1. **다음 배치에서 7차 수정 4종 검증** — `python analyze_batch.py`로 확인
   - improve ABORT("no usable plan")가 사라졌나 (구제 경로 improve-criteria-salvaged 발동 여부)
   - 인프라 장애 시 15분 대기→재개가 작동하나 (stopped_by "infra-outage" 또는 재개 로그)
   - 레벨 칸이 채워지나 → 레벨별 성공률 비교 가능해짐
2. **오답노트 A/B 검증** (선택, 쿼터 여유 있는 날): `--no-lessons` 플래그로 같은 주제 비교 런

## 6차 세션 구현분 (어젯밤 11런 분석에서 나온 개선 — 코덱스 리뷰 + 자체 분석)

### 진단 (로그 근거)
- ABORTED 7건 중 다수가 **시험 8/9 통과 상태에서 폐기** — 게이트는 100%만 인정하는데
  채점표는 부분 점수를 인정하는 비대칭. 버린 비용이 전체의 절반
- "진전 없음" 중단의 실체 = 예산 부족이 아니라 **권한 부족**: 26B는 시험지를 못 고치니
  포맷 분쟁(1.0 vs 1)에 끼면 몽키패치뿐 → 게이트가 막음 → 제자리걸음
- improve 모드가 최강 효율(신규 대비 1/8~1/10 비용)인데 수동 피드백만 받던 상태

### 부분 합격 출하 (orchestrator)
- 수리 예산 소진 시: 성공 신호 통과 + pytest 통과율 ≥80%면 ABORT 대신 출하
- 상태 "OK (partial)", 떨어진 기준은 index의 `failed_criteria`로 기록 → improve 표적

### 31B 계약 중재 (런당 1회)
- 단언 실패(AssertionError)가 반복돼 막히면: 31B에게 계약+시험지+실제 출력을 주고
  "시험이 과한가, 코드가 위반인가" 판정
- 시험 과잉 → 31B가 시험지 수리 / 코드 위반 → 지시 포함 표적 수정 후 재시도

### improve 자동 루프 (batch 우선순위 개편)
- 회차마다: ① 부분 실패 OK 런 있으면 improve (피드백 = 떨어진 기준, 작성 콜 0)
  → ② 총평 안 받은 만점 런 있으면 31B 사용자 시점 총평 1콜(reviewer.py, 코드 비공개
  블랙박스 — 자가채점 편향 차단) → 지적 시 improve, NOCHANGE면 신규 생산
  → ③ 신규 출제·생산
- 폭주 방지: 런당 자동 improve 1회(improved_from로 판별), 개선의 개선 금지,
  총평 런당 1회(auto_review.json 마커 — NOCHANGE도 기록)

### 시험지 출제 규칙 강화 (prompts)
- 전체 stdout 일치 단언 금지 / 숫자는 파싱해서 pytest.approx 비교 /
  계약이 포맷을 안 박았으면 값의 존재만 단언 — "1.0" vs "1" 류 분쟁 원천 차단

### 재발률 키워드 정규화 (run_index)
- 'data-processing'='data processing', 'cli tool'⊃'cli' 표기 차이를 토큰 단위로 흡수,
  범용 단어(cli/tool 등)는 가짜 겹침 방지를 위해 제외

## 5차 세션 구현분 (3층: 자동 출제기 + 배치)

### 출제기 (`idea_factory.py`)
- 깃허브 스타순 수집(GITHUB_TOKEN, 콜 0) → topic 버킷 25개 추첨 → 자카드 중복제거
  (과거 index.json 아이디어 대비 0.35 상한 + 사용한 저장소 재사용 금지)
- 난이도 5축(state/integration/input/modules/breadth) × 레벨 1~5 앵커 문구,
  축별 ±1 흔들어 조합 다양성. 레벨은 최근 5런 성적으로 적응 조절
  (전부 성공+수정<1 → ↑, 성공률<50% 또는 수정>2.5 → ↓) — 타깃: 자가수정 1~2회
- 상태는 `auto_state.json`(레벨 + 사용 저장소, 500개 캡)
- 뽑힌 후보만 README 발췌 → 31B 1콜로 한국어 아이디어 출제 (JSON 실패 시 1회 재요청)
- CLI: `--dry`(콜 0 점검) / 기본(1콜 출제)

### 배치 모드 (`batch.py`)
- 회차마다: 출제(콜 1) → orchestrator 서브프로세스 완주. 기본 3회차, 최대 20
- 중단 조건: STOP_AFTER_RUN 플래그 / 일일 쿼터(RPD) 소진 / 연속 실패 3회차(헛돌기 방지.
  회차당 내부 재도전 1회 포함 = 빌드 6연속 실패. 밤샘 배치 조기 정지 방지로 2→3 상향)

### 재발률 집계 (오답노트 효과의 공짜 증거)
- orchestrator가 주입한 lesson keyword를 index 엔트리(`lessons_injected`)에 기록
- `run_index.recurrence_stats()`: 주입된 런 중 같은 keyword 유형으로 실패한 비율
- 대시보드 헤더에 "재발 m/n" 표시 (주입 런이 생기면 나타남)

### 대시보드 연결
- `/api/auto` → `batch.py --runs N` 백그라운드 기동 (1~20 검증, 중복 시작 차단,
  시작 시 종료예약 해제). 자동 패널에 회차 수 입력 추가
- 대시보드 재시작 완료 — 새 API 가동 중

## 4차 세션 추가분 (1층 + 대시보드 한글화)

### 1층: `--improve <run_dir> --feedback "..."` (완료)
- 이전 결과물 복사 → 31B 개선 계획(JSON) → 26B 표적 수정/신규 파일 구현 → 게이트·채점표
- **회귀 방지선**: 기존 acceptance_criteria·criteria_checks는 코드가 강제 보존
  (31B가 빼먹어도 되살림). 신규 기준 1~3개 추가 출제
- **판정**: REPORT·index에 IMPROVED / NO-GAIN / REGRESSED (기준 텍스트로 신/구 구분)
- **NOCHANGE 출구**: 개선할 게 없으면 31B가 NOCHANGE → 콜 안 쓰고 즉시 종료
- 실전: 경비정산 4/5 빌드 + "Hold 폴백 버그" 피드백 → **6/6 IMPROVED, 콜 4회, $0.0074**
  (신규 생성 대비 1/10 비용 — 표적 수정 구조가 의도대로 작동)

### 대시보드 로그 한글화 (완료)
- 공장 비유: "[31B] 설계도 승인 - 부품 4개", "[26B] 부품 제작: main.py",
  "검수(시운전): 실패 -> [26B] 수리", "[31B] 품질심사 합격(LGTM) - 조기 출하"
- orchestrator에 phase 이벤트 추가 (설계/시험지/제작/품질심사 시작 시점 기록)
- 모르는 이벤트는 원문 폴백

### 대시보드 모드 버튼 (완료)
- [단일/개선/자동/종료예약] 모드 선택 → 패널 전환 → 공통 "시작" 버튼
- 단일: 아이디어 입력 → `/api/start` (launch_run)
- 개선: 성공 런(ok) 드롭다운(점수 표시, 5초 자동갱신) + 개선점 → `/api/improve` (launch_improve)
- 자동: `/api/auto` 자리만 — 3층 배치 연결 대기
- 종료예약: STOP_AFTER_RUN 토글 (자동 회차 종료 예약)

---

## 4차 세션에서 구현한 것 (2026-06-11)

### 모델별 비용 추적
- `llm.py`: 역할별(26B/31B) 토큰 분리 추적 + `cost_usd()` (오픈라우터 유료 단가 환산)
- 단가: 26B $0.06/$0.33, 31B $0.12/$0.36 (입력/출력 per 1M, thinking은 출력 과금)
- REPORT.md에 모델별 토큰·비용 + 합계 표기

### 0층: 런 인덱스 (`run_index.py`)
- `runs/index.json`: 런마다 성패·채점표·자가수정 횟수·비평 바퀴·콜·토큰·비용·실패 keyword
- improve(과거 런 조회)·적응형 난이도(최근 성적)·재발률(keyword 집계)의 토대

### 2층: 비평노트 (`critique_notes.py`)
- 오답노트와 분담: 오답노트="뭐가 깨지나"→설계(31B) 주입 / 비평노트="뭐가 코드를 좋게 하나"→구현(26B) 주입
- **수확 조건: rollback 안 되고 살아남은 비평만** (기존 rollback이 공짜 품질 필터)
- 키워드 겹침+빈도 점수화, 3회 이상 반복 지적은 주제 무관 주입(보편 규칙)
- `frequent_candidates()`: HARD_RULES 승격 후보 조회
- 실전 첫 수확 4건 완료

### 테스트 책임 경로 (31B 테스트 자가수리)
- 실패 원인: 31B가 쓴 test_acceptance.py 자체의 버그(CliRunner 환각 인자 등)일 때
  26B가 멀쩡한 코드만 고치다 예산 소진하던 문제
- pytest 에러의 마지막 프레임이 테스트 파일/라이브러리 내부 + TypeError류
  (AssertionError 제외, 프로젝트 파일 에러 있으면 제외)면 → 31B에게 테스트 수리 1회
- tests_prompt 규칙 추가: `CliRunner()` 인자 금지, click 안 쓰면 click.testing 금지

### 몽키패치 게이트 (gates.py `monkeypatch`)
- 26B가 깨진 테스트에 맞추려 `json.load = patched`, `processor.process_expenses = ...`
  같은 변조를 실제로 함 (두 런에서 관측) → import한 모듈(외부+로컬) 속성 대입 탐지
- `sys`만 예외 (stdout 인코딩 래핑은 정당)

### 대시보드 (`dashboard.py`)
- 콜 0, 읽기 전용 + 조작 2개. stdlib만 사용. `python dashboard.py` → 0.0.0.0:8400
- 테일스케일 IP로 폰 접속. 5초 자동 갱신
- 표시: 현재 런 RUNNING/idle + 이벤트 실시간 꼬리 + 런 이력 표(성패·점수·비용) + 누적 비용
- **작업 시작 버튼**: 아이디어 입력 → orchestrator 백그라운드 기동 (중복 시작 차단)
- **종료 예약 버튼**: STOP_AFTER_RUN 플래그 토글 — orchestrator가 회차 끝에 확인,
  자동 재도전도 안 잡음. 수동 시작 시 자동 해제. 배치 모드도 이 플래그를 쓰면 됨
- **시작프로그램 등록**: `%APPDATA%\...\Startup\arag-dashboard.vbs` (작업 스케줄러는
  관리자 권한 거부 → 시작프로그램 폴더 방식. pythonw로 창 없이 기동)

---

## 검증 현황

### 유닛 테스트: 109개 통과

### 엔드투엔드 (어려운 주제: 경비 정산 CLI — 5축 복합)
| 시도 | 결과 | 진단 |
|------|------|------|
| 1차 (수정 전) | FAIL | 31B 테스트가 click API 환각 → 26B가 멀쩡한 코드 고치다 소진. 26B가 json.load 몽키패치 시도 |
| 2차 (수정 후) 1차 | FAIL | 계약 불일치 (모듈 함수를 클래스 메서드로 구현) — 새 유형 |
| 2차 (수정 후) retry | **OK 4/5** | 자가수정 4회 → 게이트 통과 → 비평 1라운드 수정 생존 → LGTM 조기 종료 |

- **expect_exit_code 실전 검증**: "키 없이 live = exit 1" 기준 PASS (지난 세션 버그 수정 확인)
- 유일한 FAIL: 정책 미정의 카테고리 기본값이 Approved 아닌 Hold (의미적 엣지)
- 비용: 성공 런 $0.078 (26B $0.065 + 31B $0.013 — 26B thinking 145K가 비용의 83%)

---

## 합의된 설계 방향 (이번 세션 논의)

- **오답노트 효과 측정**: 강한 증거=A/B 런, 공짜 증거=재발률(배치가 쌓일수록 정밀)
- **난이도는 주제가 아니라 요구사항의 속성** — 주제(버킷 추첨)와 난이도(5축 레벨)를 분리 출제
- **개선 루프의 종료 조건이 핵심**: 채점표가 한번 만점이면 잴 게 없음 → improve 모드는
  기존 기준 유지 + 새 기준 추가로 "개선"을 숫자로 정의
- 비평노트 고빈도 패턴은 HARD_RULES 승격 → 프롬프트에 박히면 그 비평은 영구 소멸(콜 0 해결)

## 기록
- 1차 완료: 2026-06-11 / 2차 D-2 완료: 2026-06-11 / 자가개선 0·2층 완료: 2026-06-11
- 주요 교훈 (누적):
  - prompts.py HARD_RULES와 gates.py 화이트리스트는 항상 동기화
  - click 테스트는 CliRunner로만 + CliRunner는 인자 없이 (환각 인자 실관측)
  - 26B는 테스트를 못 고치게 하면 코드를 몽키패치해서라도 끼워맞춘다 → 게이트 필요
  - 26B thinking 토큰이 비용의 대부분 — 프롬프트 단순화가 곧 비용 절감
  - gemma-4-26b 500 오류 시 gemini-3.1-flash-lite로 임시 대체 가능
